<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper"> 

<insert id="insertUsers" parameterType="UserEntity">
INSERT INTO USERS(ID, PASSWORD, NAME)
VALUES(#{id}, #{password}, #{name})
</insert>

<select id="selectUserById" parameterType="String" resultType="UserEntity">
SELECT ID, PASSWORD, NAME
FROM USERS
WHERE ID = #{id}
</select>

<select id="selectUserByIdAndPw" parameterType="UserEntity" resultType="UserEntity">
SELECT ID, PASSWORD, NAME
FROM USERS
WHERE ID = #{id} AND PASSWORD = #{password}
</select>
</mapper>