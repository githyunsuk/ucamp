<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ucamp.mapper.ProductMapper">

	<select id="selectAllProduct" resultType="ProductEntity">
		SELECT PID, PNAME, CID, PRICE
		FROM PRODUCT
	</select>
	
	<select id="selectProductByCategory" parameterType="String" resultType="ProductEntity">
		SELECT PID, PNAME, CID, PRICE
		FROM PRODUCT
		WHERE CID = #{cid}
	</select>
	
	<select id="selectProductById" parameterType="String" resultType="ProductEntity">
		SELECT PID, PNAME, CID, PRICE
		FROM PRODUCT
		WHERE PID = #{pid}
	</select>
	
	<insert id="insertProduct" parameterType="ProductEntity">
		INSERT INTO PRODUCT(PID, PNAME, CID, PRICE)
		VALUES(#{pid}, #{pname}, #{cid}, #{price})
	</insert>

	<update id="updateProduct" parameterType="ProductEntity">
		UPDATE PRODUCT
		SET PNAME = #{pname}, PRICE = #{price}, CID = #{cid}
		WHERE PID = #{pid} 
	</update>
	
	<delete id="deleteProduct" parameterType="String">
		DELETE FROM PRODUCT
		WHERE PID = #{pid}
	</delete>
</mapper>
	